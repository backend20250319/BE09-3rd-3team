spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/backend20250319/studyit-config.git

server:
  port: 8888

encrypt:
  key-store:
    location: classpath:keystore.p12
    type: PKCS12
    alias: key
    password: ${KEYSTORE_PASSWORD}
